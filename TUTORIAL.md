# ce

```
-- This is a comment.

-- Ce uses space-indented blocks like Python.

data Bool:      -- a new data type with two constructors
    False ()    --  they carry no extra info
    True  ()    --  with the unit type ()

func not

val x :: Bool = False   -- a constant declaration with type and initialization
```
